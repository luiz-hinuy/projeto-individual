<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Details</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <div class="container">
        <h1>Booking Details</h1>
        <div class="booking-details">
            <p><strong>Room ID:</strong> <%= booking.rooms_id %></p>
            <p><strong>User ID:</strong> <%= booking.user_id %></p>
            <p><strong>Start Time:</strong> <%= booking.start_time %></p>
            <p><strong>End Time:</strong> <%= booking.end_time %></p>
        </div>
        <a href="/bookings/edit/<%= booking.id %>" class="btn">Edit Booking</a>
        <form action="/bookings/delete/<%= booking.id %>" method="POST" style="display:inline;">
            <button type="submit" class="btn btn-danger">Delete Booking</button>
        </form>
        <a href="/bookings" class="btn">Back to Bookings</a>
    </div>
    <script src="/js/bookings.js"></script>
</body>
</html>